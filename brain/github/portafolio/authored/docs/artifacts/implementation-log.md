<!-- generatedBy: brain:sync-github; source: https://github.com/uset82/portafolio/blob/main/docs/artifacts/implementation-log.md; checkedOn: 2026-07-31; redactions: 0 -->

# Implementation log

Date: 2026-07-19  
Slice: semantic poster-first foundation

## Implemented

- Shared semantic shell with skip link, CC identity, desktop/mobile navigation, active routes, footer, and contact path.
- Approved home copy and layered Observatory poster composition.
- Real DOM Selected Systems links for ASTRAEA, PINÁCULO, and Future Energy.
- Responsive route shells for Work, Laboratory, Sound, Cosmos, Story, Contact, three named system pages, and 404 recovery.
- Locked natural palette and typography/layout/motion tokens.
- Motion root configured with `reducedMotion="user"`; adapted Animate UI button primitive; reading-order entrance.
- Interactive CC AI desktop panel/mobile sheet with approved prompts, a validated same-origin client transport, bounded reducer state and history, abort/stop/retry/clear controls, separate live status, exact privacy copy, actual-response model disclosure, and honest disabled/offline/rate-limit recovery. Completed JSON answers use a bounded presentation reveal without claiming provider streaming; true normalized streaming remains task 6.26.
- Editorial Laboratory, Sound, Story/Cosmos, and footer continuation below the first viewport.
- Next.js image optimization and bundled font pipeline.
- Strict runtime schemas for projects, media, experience, education, trips, hobbies, links, sources, metadata, and the planning inventory.
- A 26-entry content inventory that names every known gap, fallback, rights state, owner, and launch impact without publishing unverified facts.
- Server-parsed homepage identity, actions, navigation, and Selected Systems records; reference projects remain unpublished concepts.
- Automated content and palette contract checks, including deliberate invalid fixtures and forbidden-color sentinels.
- Shared DOM/Three.js palette constants covering 30 approved colors and 11 future material roles.
- Responsive container, section, stack, cluster, 12/8/4-column grid, media-frame, prose, and visually-hidden layout primitives.
- Typed action link/button, editorial heading, status tag, optimized image frame, and editorial link-row primitives using the approved palette and interaction states.
- Homepage actions, Observatory poster, Selected Systems links, route headings, and concept status now exercise the shared UI primitives rather than duplicating markup.
- Native audio/video foundation with manual mute-first playback, no preload, required video posters, optional caption tracks, transcripts, visible credits/rights, and adjacent error recovery.
- External provider foundation that creates no iframe before explicit consent, then uses a lazy named sandboxed frame with a persistent privacy notice, load status, retry state, and approved external fallback.
- Sound route source-readiness ledger that truthfully shows the publication requirements while approved tracks, posters, captions, transcripts, and provider URLs remain unavailable.
- Four Node baseline tests covering the approved content set, video accessibility failures, autoplay embed rejection, and the Sound route's semantic no-media fallback.
- Pull-request verification workflow with pinned Node/pnpm, frozen-lockfile installation, pnpm download caching, concurrency cancellation, read-only repository permissions, and the complete `pnpm verify` gate.
- Exact `@openrouter/sdk` 0.13.65 dependency with a lazy `server-only` factory, required-key failure, normalized HTTP referrer, approved CC AI app title, unsafe-URL rejection, and no debug logger.
- Mockable OpenRouter construction plus four focused configuration/factory tests and an automated source-boundary check that rejects runtime/key use in client components.
- Disabled-by-default `POST /api/cc-ai` route with strict JSON/history/locale validation, server request IDs, no-store responses, explicit non-streaming behavior, disconnect/timeout aborts, provider token and response-character caps, and requested/responding model disclosure.
- Provider-neutral CC AI service and OpenRouter chat adapter with safe 402/429/configuration/timeout/abort/provider/invalid-response normalization; tests use only deterministic providers and no message bodies or raw SDK errors are logged.
- Server-configured CC AI model policy with a variable `openrouter/free` prototype default, named override, deduplicated ordered fallback sequence, actual responding-model metadata, and a fail-closed paid production switch.
- OpenRouter request routing denies providers that collect prompts for training in every mode; paid production routing also requires zero-data-retention endpoints, while the free prototype router omits that stricter filter so it can retain eligible endpoints. Provider fallback remains inside the active mode's privacy filters.
- Same-origin CC AI request guard with Origin/Referer and Fetch Metadata validation, trusted hosting IP plus server-issued HttpOnly session limits, one-active-request-per-session and process concurrency ceilings, bounded state, `Retry-After`, and distinct forbidden/rate/busy responses.
- Provider work remains single-attempt: model/provider fallback occurs inside one bounded OpenRouter request, while visitor retries are explicit and pass through the local limit guard.
- Public-only CC AI knowledge builder over the validated content model: factual approval, publication, rights, and all-public-source gates exclude private/design-only/held records without reading provenance files.
- Deterministic whole-record JSON context capped at 8,000 characters, traceable public source IDs, response knowledge metadata, status-preservation rules, prompt-injection boundary, and an exact honest-unknown instruction. The current approved context correctly contains zero facts.
- Clean-checkout foundation proof for exact commit `71368ad`: frozen-lockfile install, all 40 tests and standard checks, 13-page production build, desktop/mobile production inspection, overflow/image/console checks, and the mobile navigation keyboard path.
- Exact compatible Three.js runtime set: Three.js 0.185.1, React Three Fiber 9.6.1, Drei 10.7.7, and matching Three.js types on React 19.2.4.
- Reusable `LazyThreeCanvas` project boundary with SSR disabled, an internal renderer owner, bounded DPR, demand rendering, transparent antialiasing, no preserved drawing buffer/default shadows, caller-owned accessible fallback, and a named `useGLTF`/`useKTX2`/`Preload`/`PerformanceMonitor` Drei allowlist.
- Three runtime contract tests bring the deterministic suite to 43; the full production build passes and its current initial client chunks contain no Three.js runtime markers because no scene is mounted yet.
- Typed Zod contract plus bundle-safe registry data for all 12 Observatory assets, including stable IDs, meter scale, planned nodes/clips, material roles, per-LOD triangle budgets and nullable URLs, poster/DOM fallbacks, interaction targets, explicit provenance/copyright gaps, and hero/deferred/on-demand priority.
- Registry-to-manifest tests prove ID and LOD parity, local fallback existence, palette-role integrity, and fail-closed behavior: no current candidate has a public GLB URL, and adding one before runtime/provenance approval is rejected.
- Self-hosted Three.js 0.185.1 Draco and Basis/KTX2 decoder files with pinned hashes and the upstream MIT license; no mutable decoder CDN or version mixing.
- Shared glTF loader configuration attaches one reusable Draco instance, meshopt, renderer-detected KTX2, and two-worker ceilings; each explicit attempt uses a fresh LoadingManager and a first-fatal-error latch so retry/progress state cannot drift.
- Scoped GLB cache eviction, abort support, permanent decoder teardown, and deduplicated cleanup for geometries, materials, textures, closable image bitmaps, and skeletons. Five focused loader tests and the tightened public-rights registry contract bring the deterministic suite to 53.
- Offline GLB gate using pinned glTF Transform, Khronos glTF Validator, Draco, and meshoptimizer tooling. It reports source hash/bytes, official validity, meter dimensions, transforms, required nodes/clips, materials, textures, visible triangles, draw calls, extensions, and per-registry/manifest budgets.
- Deterministic Meshopt variant generation preserves the contract, validates before writing, emits a pinned-toolchain/input/output hash sidecar, refuses implicit overwrite, and supports byte-for-byte regeneration checks. Five synthetic-fixture tests bring the deterministic suite to 58 while the public registry correctly remains empty.
- Typed Observatory scene configuration assigns all 12 registry assets exactly once across seven named groups and defines provisional storyboard camera views, a locked-palette no-shadow local light rig, a transparent sRGB environment with no external map, and explicit renderer/camera/scene/state/frame/DOM ownership.
- An external reducer/store owns quality, motion/pause/visibility, muted scene sound, real loading progress and safe failure/retry states, selected artifact, and monotonic cancellable camera requests. The R3F shell projects snapshots without `useFrame` or React state updates in the frame loop; eight focused tests bring the deterministic suite to 66 while the shell remains unmounted.
- The unmounted water group now owns a locked-palette procedural shader with shallow wave displacement, analytic reflected light, and demand invalidation capped at 30 updates per second. Reduced motion/quality uses one still two-triangle standard material and the static path renders nothing; four contract tests bring the suite to 70 without a browser or performance claim.
- An outside-Canvas capability controller now resolves WebGL2, viewport, DPR, optional memory/CPU, reduced motion/data, connection, battery, visibility, and manual preference through one fail-closed policy. Version 2 scene state owns signals/reasons, and an unmounted semantic Auto/Full/Reduced/Poster fieldset completes the manual path; eight tests bring the suite to 78.
- The homepage now mounts a poster-first progressive boundary without mounting Canvas while the current critical model URLs remain rights-gated. A typed quality-aware plan selects full/reduced LODs, requires every remaining critical model, then stages sequential critical, idle-deferred, and selection-driven on-demand attempts behind dynamic client boundaries; safe status, abort, disposal, and scoped cache eviction bring the deterministic suite to 83. Browser no-blank and real-model evidence remain open.
- Shared immersive test doubles now derive a schema-valid repository-only model plan, inject in-memory GLTF success/failure/abort attempts through the production loader contract, and script deterministic CC AI success/error replies through the real route handler. The chat helper also emits a serializable browser-interception payload; seven focused contracts bring the suite to 90, the 13-route production build passes, and emitted client chunks contain no test-double markers.
- The verification workflow now separates prebuild contracts/tests, the production build, and a post-build immersive artifact gate. The final stage validates manifest/registry metadata and budgets, every public GLB, emitted client files for AI secrets/test markers, and the actual prerendered homepage poster fallback. That inspection found Motion's no-script opacity gap; a no-script-only layout override preserves the approved static hierarchy while JavaScript keeps the restrained reveal. The complete 90-test, 13-route `pnpm verify` chain passes locally; the first hosted run remains the next gate.
- A reusable Natural Observatory case-study layout now replaces the generic named-system intro. Its concept branch publishes no unverified project facts or media; its verified branch supports the approved contribution-to-related-work order, rights-gated image/audio/video/embed evidence, code or architecture and demo slots, outcomes, learnings, stack, and verified source actions. Two static-render contracts plus 1440×900 and 390×844 browser inspection cover hierarchy, held-state suppression, 44px actions, single-column reflow, overflow, and console state; the complete gate now passes 98 tests and the 13-route production build.
- The identity-led homepage hero now fails open as semantic server-rendered markup instead of depending on client animation to become visible. Approved public-profile provenance backs Carlos Carpio's name and role; one headline, one short support statement, and two distinct 44px actions lead into the real Observatory poster. Tokenized CSS provides the restrained stagger under the global reduced-motion override. A deliberate mobile crop keeps the robot and instruments—not the poster's embedded desktop interface—inside the first viewport, while the optional current-focus note follows the scene. Two new contracts, 1440×900 and 390×844 browser inspection, and the complete 100-test/13-route verification gate pass.
- A publication-ready `SelectedWork` component now expresses the verified five-project draft order as a cardless editorial index: code-native typographic plates replace unapproved screenshots, while category, literal maturity status, contribution boundary, and a 44px action remain visible without hover. Focus-within, pointer hover, pressed touch feedback, and single-column mobile reflow are explicit. A temporary local-only preview was inspected at 1440×900 and 390×844, then removed; the final component remains unmounted pending task U.4 approval. Two contracts bring the complete verification gate to 102 tests while the production build remains 13 routes.
- The homepage profile/CV teaser now uses the exact approved public role and long biography, three approved practice threads, a clear `/story` path, and the verified public GitHub profile. A code-native `CC` profile study supplies the approved no-portrait fallback without publishing the private résumé, portrait, contact details, or unsupported career facts. The wide cardless field becomes a copy-first single mobile column; all thread rows and actions retain 44px sizing, and the global reduced-motion path collapses its restrained transition. Two focused contracts, 1440×900 and 390×844 browser inspection, and the complete 105-test/13-route verification gate pass.
- The named-system pages now end with a cardless Observatory relay derived from the approved ASTRAEA → PINÁCULO → Future Energy order. The visual thesis is one divided field-guide index; the content plan is adjacent system identity plus an explicit Work exit; the interaction thesis is fast directional-arrow feedback with the global reduced-motion collapse. First, middle, and last states omit the current route and never wrap, while semantic labels expose direction and project name without publishing held evidence. Four focused contracts, real route activation, 1440×900 and 390×844 browser inspection, and the complete 109-test/13-route verification gate pass.
- The homepage Sound + moving-image teaser now uses a silent score on sage paper as its visual thesis: one code-native instrument balances an approval-backed copy field without inventing cover art or playback. The content hierarchy is source-readiness status, identity, publication boundary, Music and Moving image formats, then an honest `/sound` path; the interaction thesis is restrained instrument/action response with the global reduced-motion collapse. The composition changes from a wide score/copy field to copy-first mobile, creates no audio/video/iframe/autoplay surface, and keeps actual media gated by tasks 5.18 and U.6. Two focused contracts, real `/sound` activation, 1440×900 and 390×844 browser inspection, and the complete 112-test/13-route/25-client-file verification gate pass.
- The homepage Travel + personal-practice teaser now uses a buff field-notes spread as its visual thesis: an abstract contour/horizon study adds personal atmosphere without using an unapproved photograph or implying a real itinerary. The content hierarchy is privacy/source status, personal-practice identity, publication boundary, three approved Cosmos themes, then an honest `/cosmos` path; the interaction thesis is restrained field/marker movement with the global reduced-motion collapse. The wide copy/field composition becomes copy-first mobile, publishes no precise location/date/trip/media claim, and keeps actual stories and galleries gated by task 5.19. Two focused contracts, real `/cosmos` activation, 1440×900 and 390×844 browser inspection, and the complete 115-test/13-route/25-client-file verification gate pass. Browser QA also identified and resolved Next.js’s smooth-scroll opt-in warning through the root HTML contract.
- The site footer now uses a deep-espresso last-light field as its visual thesis: one oversized invitation and code-native contact signal resolve the Natural Observatory sequence without another card. The content hierarchy is privacy-pending status, concise invitation, internal Contact CTA, verified GitHub path, five unique route exits, then copyright; the interaction thesis is restrained signal/route movement with the global reduced-motion collapse. The footer receives validated content from the server layout, omits the duplicate Contact route from its index, becomes CTA-first on mobile, and adds bottom clearance for the fixed CC AI trigger. No email, form, phone, address, availability, or booking claim is published while task 5.20 remains open. Three focused contracts, real `/contact` activation, 1440×900 and 390×844 browser inspection, and the complete 118-test/13-route/23-client-file verification gate pass.
- The Work route now uses a warm archival systems register as its visual thesis: a large evidence-boundary introduction resolves into three full-width instrument rows rather than a card grid. The content hierarchy is validated-route count, explicit concept/publication boundary, ordered ASTRAEA/PINÁCULO/Future Energy identity, accurate group/category taxonomy, literal status, bounded summary, and one internal action; the interaction thesis is static-first readability with restrained row/instrument feedback under the global reduced-motion path. It consumes only the three validated site records and keeps all five publication-held flagship drafts and contribution claims out of runtime copy. Two focused contracts, real `/work/astraea` activation, 1440×900 and 390×844 browser inspection, and the complete 120-test/13-route/23-client-file verification gate pass.
- The layered Observatory shell now has explicit semantic ownership without changing its approved composition or asset policy. `ObservatoryProgressiveExperience` is a labeled figure with ordered poster, conditional Canvas, and live-status layers; the poster remains exposed until the external scene lifecycle reaches ready, while the Canvas receives the same approved scene description and becomes the sole visual description only at genuine readiness. `ThreeCanvas` keeps client-only, bounded-DPR, demand-rendered ownership and now accepts an accessible description ID. Two new source/runtime contracts cover header → scene → identity/CTAs → CC AI → Selected Systems → continuation order, hero-confined poster use, z-layering, palette, focus/touch sizing, reduced motion, and narrow reflow. The complete 122-test/13-route/23-client-file gate passes with no new GLB or provider call; task 5.23 remains open pending separately authorized browser QA and a rights-cleared real Canvas presentation.
- The Observatory environment is now a rights-safe procedural cutaway rather than another blocked external model. A shared material kit and quality contract define linen plaster, off-white floor, buff trim, sage window glass, walnut ribs, pewter basin/oculus detail, slate water depth, and one controlled espresso shadow role. The full/reduced geometry builds an open circular wall and roof annulus, five/three round window bays, eleven/seven ribs, nine/five radial oculus beams, a translucent skylight, and an elliptical basin while remaining under declared 58/38 draw calls, 18,000/7,500 triangles, nine materials, and zero textures, HDR, shadows, or post passes. `ThreeCanvas` now owns sRGB/ACES exposure, the warm local light rig is fully runtime-authored, and the registry leaves only the robot model as a critical rights gate. Four new environment contracts bring the complete non-browser gate to 126 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files; task 5.24 remains open pending separately authorized live desktop quality-tier inspection and renderer diagnostics.
- The Observatory robot now has one production integration path without weakening the rights gate. A technical-art contract validates registry-named body, head, hand, contact, and interaction nodes; the three required clips; finite bounds; meter/Y-up basis; and declared mesh/material/texture/file budgets before readiness. A presentation wrapper—not the canonical asset—owns uniform normalization, base pivot, yaw, focus metadata, and exact hand-to-water placement. Asset-ID presentation state is separate from URL-keyed disposal ownership; the guide group mounts only validated assets, selection updates the external scene store, and the primitive leaves resource disposal with the loader owner. Full motion runs the idle mixer through demand invalidation capped at 24 updates per second, while reduced, paused, and hidden modes hold the pose. Four deterministic contracts cover success, spatial alignment, malformed fallback, and runtime ownership. The complete non-browser gate passes 130 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files. Task 5.25 remains open because no rights-cleared optimized robot, approved material/reflection treatment, live browser inspection, or measured renderer evidence exists yet.
- The Observatory water surface now has one bounded interaction owner instead of one hard-coded ripple. A five-slot controller reserves one repeating 1.2-second impulse for the robot-hand contact from the shared spatial contract and caps primary pointer/touch presses at four 1.2-second transient impulses with a 140ms cooldown and inset hit bounds. Pointer hits are converted once from world to water-local space, then written directly to fixed shader uniforms; no React or scene-store frame state was added. The full tier retains one 6,912-triangle draw/material/geometry, zero textures/render targets/post/shadows, and at most 30 demand invalidations per second while adding a constant five-iteration ripple field, analytic Fresnel reflection/glint, and a no-sampling analytic refraction-depth cue. Paused/hidden state freezes time and input, reduced motion/quality remains a still two-triangle surface, and static/error/unsupported paths retain the poster. Two controller contracts bring the complete non-browser gate to 132 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files. Task 5.26 remains open pending approval-gated shader compilation, real desktop pointer behavior, visual hand contact/decay/reflection review, renderer measurements, and separately approved mobile touch QA.
- ASTRAEA is now a runtime-authored celestial chart mechanism instead of another external-model dependency. Its natural-palette technical-art contract defines parchment ceramic, aged pewter, walnut, and muted sage; three marked rings, radial lines, fasteners, a focus pointer, and a plausible stand; and explicit prohibitions on glyphs, pseudo-writing, screen UI, neon, and particles. Mesh and DOM focus both select the external `astraea` state and existing camera view. Full selection runs one interruptible 760ms ring/pointer alignment at no more than 24 demand invalidations per second and then stops; reduced motion/quality and narrow viewports resolve immediately to a still two-ring mechanism, while poster/error/static paths keep the server-validated Concept label and `/work/astraea` link. Declared full/reduced limits are 5,000/1,800 triangles, 24/17 draw calls, four materials, and zero textures/render targets/post/shadows. Four deterministic contracts bring the complete non-browser gate to 136 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files. Task 5.27 remains open because the critical robot rights gate still prevents production Canvas mounting and no desktop visual/input/camera/renderer or separately authorized mobile/touch QA has run.
- PINÁCULO is now a runtime-authored, instanced 24-position tabletop mechanism instead of an external-model dependency. Its walnut, clay, buff, and espresso technical-art contract uses one marker carrier, concentric construction grooves, a fixed latch, and twelve shallow marker grooves arranged as paired one-, two-, and three-groove stations to reference 11/22/33 without numeric or glyph geometry. Mesh and generic DOM focus select the external `pinaculo` state and owned low camera view; one interruptible 700ms action advances the carrier by exactly one 15-degree position and lifts the latch at no more than 24 demand invalidations per second, then stops. Reduced quality/motion and narrow viewports preserve all 24 positions and settle immediately; poster/static/error paths keep the server-validated Concept identity and internal project path, with the existing Selected Systems link retaining the narrow-screen DOM destination. Declared full/reduced limits are 5,600/2,400 triangles, 12/11 draw calls, four shared materials, and zero textures/render targets/post/shadows. Four deterministic contracts bring the complete non-browser gate to 140 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files. Task 5.28 remains open because the critical robot rights gate still prevents production Canvas mounting and no desktop visual/input/camera/renderer or separately authorized mobile/touch QA has run.
- Sound Lab is now a runtime-authored, sparse harmonic instrument rather than another model dependency. Its walnut, buff, graphite, aged-pewter, and muted-sage contract creates one central dial, four tactile knobs, two sliders, instanced fasteners, and a 700ms focus-only dial/slider gesture capped at 24 demand invalidations per second. Reduced quality/motion retains two knobs and one slider in a still pose; poster/static/error states keep a DOM focus equivalent, truthful approval status, and `/sound` destination. The audio catalog remains deliberately empty and requires complete title, artist/credit, duration, rights/availability, transcript/notes, source, and waveform metadata before admission; playback is manual, mute-first, no-autoplay, `preload="none"`, and unavailable today. The future analyzer rejects amplitude unless approved playback is actually playing, unmuted, fully animated, and visible, so no placeholder audio, waveform, or fake response entered the bundle. Declared full/reduced limits are 6,500/2,700 triangles, 18/14 draw calls, five materials, and zero textures/render targets/post/shadows. Five deterministic contracts bring the complete non-browser gate to 145 tests, 13 routes, 12 manifest assets, 0 public GLBs, and 23 client files. Task 5.29 remains open pending approved Sound inputs/metadata, real player keyboard/touch behavior, the robot rights gate, and separately authorized browser/mobile QA.
- The homepage now exposes one unified Experience disclosure instead of an unmounted quality-only fieldset and passive header statuses. Version 3 scene state owns Auto/Full/Reduced/Poster quality, system/explicit reduced motion, pause, mute-first sound status, atomic apply/reset, and capability resolution; a strict versioned local-storage contract persists only explicit quality/motion/pause choices and synchronizes tabs without storing sound or device signals. The desktop drawer is header-linked and layered above the scene/CC AI launcher; mobile places the complete control after the poster in normal flow. Five focused tests bring the complete suite to 170. Production-browser inspection at 1440×900 and 390×844 verified 44px targets, manual Poster/Reduce/Pause, reload persistence, Reset, Escape/Close focus return, reachable sound/following content, no clipping/overflow, and an empty warning/error log while the rights-gated poster remained authoritative. The final 13-route build and 12-manifest/0-public-GLB/25-client-file immersive gate pass.
- The CC AI presentation is now production-browser verified rather than source-only. Desktop 1440×900 inspection covered the natural-palette launcher/panel, prompt and Enter submission, exact privacy disclosure, honest disabled-service alert with Work/Story recovery, Clear, and Escape focus return. Mobile 390×844 inspection covered the 78svh modal sheet, safe viewport bounds, body lock, all visible controls at least 44px, two-way focus wrapping through the privacy summary, deterministic recovery, no overflow, and focus/scroll restoration. QA removed transform ownership from the route-entry shell so fixed overlays stay viewport-bound, separated the assistant launcher from the in-flow Experience control, corrected their stacking, and admitted the native privacy summary to the focus boundary. All 170 tests and the 13-route/12-manifest/0-public-GLB/25-client-file gate pass without a provider request; normalized provider streaming remains task 6.26.
- The layered Observatory shell now has approved 1440×900 desktop and separately approved 390×844 mobile live-Canvas passes using repository-only in-memory GLTF doubles through the production progressive-loading owner. Desktop Full/Reduced reached a truthful ready state with a 1425×900 DPR-1 Canvas; mobile Auto/Reduced used a 375×212 DPR-1 Canvas; both matched client/scroll widths and retained nonblank Poster/exact-failure states. Desktop ASTRAEA focus/return synchronized DOM, camera, announcement, and URL; mobile retained a 192×44px case-study target that successfully opened `/work/astraea`. Visual inspection moved walnut ribs/beams to a protected rear arc, fixed the reserved-GLSL water identifier and ready copy, then removed a measured 17.9px ASTRAEA/CC AI overlap and moved the clipped mobile status inside its scene. Fresh tabs had no page or shader error, with only the tracked Fiber/Three `THREE.Clock` warning. The temporary QA route was removed; the public route remains poster-only until the robot rights gate is satisfied. Final verification passed all 170 tests, all validation gates, 13 production routes, 12 manifest assets, zero public GLBs, 25 client files, and one semantic poster fallback.
- Observatory renderer evidence now has one opt-in, serializable owner instead of relying on estimates or an ad hoc global. The diagnostics facade captures CSS/drawing-buffer size, DPR, last-completed-frame calls/triangles/points/lines, GPU resource/program counts, and unique visible scene geometry/material counts; its 250–5,000ms activity sample compares the renderer frame counter without invalidating the demand loop. A scenario report requires production/development mode, browser, desktop/mobile class, route, viewport/DPR, quality, motion, selection, and console counts; it checks the approved Full/Reduced calls, triangles, and materials budgets with exact overages and rejects development, console-error, or over-budget captures as evidence. Runtime budgets are parity-tested against the provider-neutral manifest while CPU/GPU frame time and texture-memory MB remain explicit limitations. It is forwarded only when a QA consumer requests it, mounts no debug UI, and publishes no `window` global. Seven diagnostics contracts bring the complete gate to 177 tests; formatting, zero-warning lint, strict types, content/palette/server/asset checks, the 13-route production build, and the 12-manifest/0-public-GLB/25-client-file semantic fallback scan pass. Production-mode renderer measurements remain unclaimed pending a separately authorized scenario.
- The water controller now exposes an equally opt-in, serializable evidence seam for the remaining task 5.26 interaction checks. A capture reports presentation tier, shader time, whether input is enabled, robot origin/age/cycle, active pointer slot origin/age/normalized decay/remaining life/envelope, accepted slot counts, and distinct non-finite, edge-bound, and cooldown rejections. Frozen shader captures preserve their measured state with input disabled; reduced and poster paths expose no controller or invented time. The callback is forwarded only to an explicit QA consumer, publishes no global, mounts no debug UI, forces no invalidation, and adds no loop. Two focused contracts bring the complete gate to 179 tests; formatting, zero-warning lint, strict types, all content/palette/server/asset checks, the 13-route build, and the 12-manifest/0-public-GLB/25-client-file immersive scan pass. Task 5.26 remains open because real pointer/touch origin and decay, pause/visibility, visual reflection/refraction, a rights-cleared robot, and production renderer/profiler measurements are still unobserved.
- The Aerial systems runtime now exposes the same evidence quality for task 5.31 without advancing its locked visual-fidelity gate. Its opt-in serializable capture distinguishes active, resting, paused, reduced, and poster phases and records elapsed cycle time, the exact last-applied offset/attitude/rotor pose, world position, corridor margin, actual and required roof clearance, robot-exclusion distance/margin, and attitude margin. Reduced captures resolve to the authored rest pose; poster captures expose no invented runtime state. The callback is forwarded only when a QA consumer requests it and adds no global, debug UI, forced invalidation, or second loop. One focused contract brings the complete gate to 180 tests; formatting, zero-warning lint, strict types, all content/palette/server/asset checks, the 13-route build, and the 12-manifest/0-public-GLB/25-client-file immersive scan pass. Task 5.31 remains open because reference comparison, rotor/body attachment, camera framing, label occlusion, selection, live visibility pause, and renderer/profiler behavior remain unobserved.
- Observatory camera navigation now has a serializable evidence seam attached to its existing single owner. The opt-in capture reports whether the camera is mounted; requested view and external-store phase; animated, immediate, or paused mode; current and requested position, target, FOV, near, and far values; exact remaining deltas; active request/progress/easing/remaining time; stale-request mismatch; and animated-start, animated-complete, immediate-complete, and interruption counters. Near/far clipping now participates in settled-pose equality instead of being omitted from the alignment contract. The facade publishes no global, mounts no UI, forces no invalidation, and creates no second loop. Two focused contracts bring the complete gate to 182 tests; formatting, zero-warning lint, strict types, content/palette/server/asset checks, the 13-route build, and the 12-manifest/0-public-GLB/25-client-file immersive scan pass. Task 5.32 remains open because real focus/reset/interruption, deep-link/history/Escape/scroll journeys, visual framing, and mobile touch behavior remain unobserved.
- The immersive MVP preflight now has one coherent diagnostics bundle instead of four disconnected callbacks. An opt-in versioned facade captures renderer, camera, drone, and water state together, delegates the bounded scenario/activity report to the real renderer owner, and fails evidence closed when any owner is absent or the renderer report is ineligible. The progressive scene forwards owners only for requested legacy or unified diagnostics, Canvas teardown clears stale renderer ownership, and a replacement callback receives the existing mounted facade. No public route consumer, global, debug UI, forced invalidation, polling, or second animation loop was added. Four bundle contracts bring the complete gate to 186 tests; formatting, zero-warning lint, strict types, content/palette/server/asset checks, the 13-route production build, and the 12-manifest/0-public-GLB/25-client-file immersive scan pass. Task 5.35 remains open because the real desktop/mobile Full, Reduced, Poster, no-WebGL, and reduced-motion journeys and the rights-cleared production robot are still outstanding.
- The immersive MVP preflight now has a deterministic definition of complete evidence rather than an informal checklist. Seven scenarios cover desktop Full/Reduced/Static/no-WebGL/reduced-motion and mobile Reduced/Static, with exact viewport, DPR, capability, quality, motion, and Canvas/poster expectations. Every scenario requires the same Work, Laboratory, Sound, Cosmos, Story, Contact, Selected Systems, and CC AI entrypoints plus successful project/navigation/chat journeys, visible semantic content, a nonblank primary visual, understandable status, and no overflow. Interactive scenarios require eligible context-matched unified diagnostics, reversible Canvas focus, and nonzero CSS/drawing-buffer dimensions; poster scenarios reject diagnostics. Unknown, missing, duplicate, development, mismatched, hidden, failed, overflowed, and console-error evidence remains incomplete. Five contracts bring the complete automatic gate to 191 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Browser, screenshot, Canvas-diagnostics, production-preview, and mobile QA were not run because those scopes require explicit approval; task 5.35 and the production robot gate remain open.
- The rights-gated focal robot now supplies the missing fifth owner in the unified Observatory evidence path. Its opt-in snapshot reports contract validity and exact issues, active/paused/reduced/poster phase, selection, canonical and scaled dimensions, presentation position/yaw/scale, hand target and measured alignment error, transformed interaction/focus anchors, and idle clip availability/time/duration/cycle/24fps cap. Malformed assets expose no presentation and never claim motion; teardown clears the facade without a global, debug surface, forced invalidation, or added loop. The instrumentation also exposed and fixed an existing tier mismatch: Reduced quality previously ran the idle mixer when motion preference was Full, while the authored contract required a held pose. Idle now requires Full quality and Full motion, the five-owner unified report fails with `robot-unavailable` if the guide is absent, and the MVP assessor separately rejects invalid contract/presentation/phase/contact/idle state. Two new robot contracts bring the automatic gate to 193 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 5.25 and 5.35 remain open for a rights-cleared optimized robot and separately approved real browser evidence.
- ASTRAEA now closes the next immersive-evidence gap from its real bounded mechanism owner. Its opt-in serializable capture reports Full/Reduced/Poster presentation, settled/animating/paused/reduced/poster phase, selection, actual and target progress, the last applied three-ring angles and pointer lift, exact target pose, per-ring/max angular error, pointer error, and the settled verdict. Non-finite input and Poster mode expose no fabricated pose. The facade is forwarded only when requested, clears on teardown, and adds no global, UI, invalidation source, timer, or animation loop. The unified report now fails with `astraea-unavailable`; the MVP gate separately rejects the wrong tier/phase/selection target, absent/non-finite pose, and unsettled or out-of-tolerance ring/pointer alignment after the focus/reset journey. Two new ASTRAEA contracts bring the automatic gate to 195 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 5.27 and 5.35 remain open for separately authorized rendered evidence and the rights-cleared production robot.
- PINÁCULO now supplies equally precise evidence from its existing bounded carrier/latch owner. Its opt-in serializable capture reports Full/Reduced/Poster presentation, settled/animating/paused/reduced/poster phase, selection, actual and target progress, the static 24-position and exact `2π / 24` step contract, current/target position offset, last-applied and target carrier/latch pose, and tolerance-based alignment errors. Poster and non-finite states expose no fabricated runtime pose. The facade is forwarded only when requested, clears on teardown, and adds no global, UI, invalidation source, timer, or animation loop. The unified report now fails with `pinaculo-unavailable`; the MVP gate separately rejects an invalid position contract, wrong tier/phase/selection target, absent/non-finite pose, incorrect offset, and unsettled carrier/latch state. Two new PINÁCULO contracts bring the automatic gate to 197 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 5.28 and 5.35 remain open for separately authorized rendered evidence and the rights-cleared production robot.
- Sound Lab now supplies the eighth unified evidence owner without weakening its approval hold. Its opt-in serializable capture reports Full/Reduced/Poster presentation, settled/animating/paused/reduced/poster phase, selection, actual/target progress, last-applied and target dial rotation/lift, slider offset, signal lift, and tolerance-based alignment errors. The same snapshot proves the current catalog is empty, player unavailable, autoplay disabled, preload `none`, scene sound muted, approved amplitude zero, and audio response inactive. Poster and non-finite states expose no fabricated runtime pose; a synthetic playing amplitude remains representable only as invalid evidence. The facade is forwarded only when requested, clears on teardown, and adds no audio source, player, provider request, global, UI, invalidation source, timer, or loop. The unified report now fails with `sound-lab-unavailable`; the MVP gate rejects wrong mechanism state or any source/player/sound/amplitude contradiction. Two new Sound Lab contracts bring the automatic gate to 199 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 2.29, 5.29, U.15, and 5.35 remain open for approved audio, real player behavior, separately authorized rendered evidence, and the rights-cleared production robot.
- Future Energy and Electronics / AI now close the remaining procedural-artifact observability gap as the ninth and tenth unified evidence owners. Future Energy reports actual/target pump, liquid-surface, and service-latch pose plus progress/rotation/height errors and proves two closed independent circuits with zero cross-connections and distinct reservoir/pump/stack-port ownership. Electronics / AI reports actual/target tactile-control, service-panel, and indicator pose plus progress/rotation/height errors while preserving the immutable no-functioning-hardware, no-inference, no-live-data, protected-module, blank-indicator, and no-flashing contract. Poster and non-finite states expose no fabricated runtime pose. Both facades are forwarded only when requested, clear on teardown, and add no global, UI, provider call, timer, invalidation source, or loop. The unified report fails with `future-energy-unavailable` or `electronics-ai-unavailable`; the MVP gate rejects wrong state, circuit contradictions, and false product claims. Four new contracts bring the automatic gate to 203 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 5.30 and 5.35 remain open for separately authorized rendered evidence and the rights-cleared production robot.
- The MVP gate now validates the remaining camera, drone, and water owners independently rather than treating their presence as sufficient. Camera evidence must finish the reversible journey mounted, settled at the expected view, transition-free, within position/target/FOV/clipping tolerances, in the correct motion mode, and with at least two completed focus/reset movements. Drone evidence must use the expected tier/phase, remain finite, pass every safety inspection, and preserve the authored corridor, roof, robot-exclusion, and attitude margins. Water must resolve to the correct Shader/Simple tier, match animation/input state, expose a bounded controller only for Shader, preserve robot slot 0, limit active slots to five, and keep pointer origins normalized. Dedicated fail-closed issue codes and one new contract bring the complete gate to 204 tests, 13 routes, 12 manifest assets, zero public GLBs, and 25 client files. Tasks 5.31, 5.32, and 5.35 remain open for separately authorized rendered evidence and the rights-cleared production robot.
- Story is now an approval-backed public profile rather than a generic “content coming later” shell. A cardless first viewport pairs Carlos Carpio's exact approved name and role with the typographic `CC` no-portrait fallback; subsequent divided fields publish only the approved long biography and three practice threads, then state the web-CV boundary explicitly. Work, Contact, and the verified GitHub profile are the only actions. The page exposes no image, private résumé download, direct contact detail, location, private identity field, or unapproved experience/education/skills record. Two deterministic contracts plus the existing route privacy assertion bring the automatic gate to 206 tests; task 5.16 remains open because U.18 still owns the privacy-safe career record and representative desktop/mobile browser review was not authorized in this pass.
- Contact now has a useful privacy-first fallback instead of a generic future-content shell. Its dark last-light first viewport states that email, form, booking, and availability are unapproved; the next field exposes the approved GitHub account exactly once as a verified public profile without promising a reply, availability, employment, or booking. A four-row public-boundary ledger names the email, form, additional-social, and availability holds, while Work and Story provide context-first alternatives. The route renders no form, input, upload, address, `mailto:`, `tel:`, private value, or hidden collection path. Two deterministic contracts bring the automatic gate to 208 tests; task 5.20 remains open because the final channel/availability decision and representative rendered review are still pending.
- Sound now has a complete mute-first publication room instead of a generic preparation shell. The approved identity and source-readiness state lead into two explicit Music and Moving image format lanes, a four-part source/rights/accessibility/manual-playback/provider-consent gate, and a dark no-player boundary with Work and Contact recovery paths. Its visual system uses a code-native silent disc and score without implying a waveform, analyser, track, or performance. The route loads no media element, image, Canvas, provider, autoplay surface, or fabricated work. Two focused contracts plus the updated semantic route assertion bring the automatic gate to 210 tests; the full gate passes 13 routes, 12 manifest assets, zero public GLBs, and 26 client files. Task 5.18 remains open for approved launch media and separately authorized representative rendered review.
- Cosmos now has a privacy-first unplotted atlas instead of a generic preparation shell. Its approved identity and hold state lead into Travel notes, Astrology studies, and Numerology studies as three explicit future formats, each with a distinct publication and claims boundary; a four-part gate keeps story selection, place/time granularity, image rights/accessibility, and responsible symbolic-practice framing visible. The route exposes no journey, gallery, map, location, date, coordinate, itinerary, image, reading, or predictive result and preserves Work and Story recovery paths. Two deterministic contracts bring the automatic gate to 212 tests; the full gate passes 13 routes, 12 manifest assets, zero public GLBs, and 26 client files. Task 5.19 remains open for approved personal content and separately authorized representative rendered review.
- Laboratory now has a concept-safe open workbench instead of a generic route shell. The approved Future Energy record and a bounded Electronics / AI visual-navigation concept form two technical instruments within one cardless register; a four-part evidence ledger makes concept status, unbuilt hardware, absent inference/live data, and held media/links explicit. Future Energy retains its honest held `/work/future-energy` case-study path, while Electronics / AI receives no fabricated project route. The page publishes no functioning device, manufactured ownership, hardware performance, trained model, inference, telemetry, live data, image, media player, or Canvas claim. Two deterministic contracts bring the automatic gate to 214 tests; the full gate passes 13 routes, 12 manifest assets, zero public GLBs, and 26 client files. Task 5.22 remains open for separately authorized representative desktop/mobile journeys and rendered focus/overflow review.

- The semantic-core preflight now server-renders Work, Laboratory, Sound, Cosmos, Story, and Contact as a unified static contract. Each route must expose exactly one main skip-link target, one H1, a visible set of route-appropriate recovery destinations, and no hidden main landmark. Laboratory, Sound, Cosmos, and Contact additionally reject unapproved audio, video, iframe, Canvas, or data-collection controls. This adds two deterministic checks without pretending to replace live layout, focus, link-activation, or overflow review. The automatic gate now has 216 tests; the full gate passes 13 routes, 12 manifest assets, zero public GLBs, and 26 client files.

- The post-build immersive validator now inspects the actual emitted HTML for Home, Work, Laboratory, Sound, Cosmos, Story, and Contact. It requires one visible `main#main-content`, one H1, and the complete global navigation on every primary static route, then enforces the held Laboratory, Sound, Cosmos, and Contact no-media/no-collection boundaries in the production artifact itself. The existing homepage poster/no-script assertion remains separate, retaining its Canvas-free rights-gated fallback. A fresh full verification still passes 216 deterministic tests, 13 routes, 12 manifest assets, zero public GLBs, and 26 client files; browser review remains intentionally open.

- The published GitHub Actions workflow now has hosted proof: [run 30120350422](https://github.com/uset82/portafolio/actions/runs/30120350422) passed against `main` commit `8db4d2e`. The lockfile install, source/contracts/test stage, production build, and emitted immersive artifact/fallback stage all completed in the read-only `Format, test, and build` job. The `pull_request` trigger, lockfile-keyed pnpm cache, concurrency cancellation, and deployment separation remain intact.

- The content gate now fails closed around the eight committed but pending local Observatory/monogram files. `validate-content.ts` requires their licensing-group decision to remain `exclude` and scans every runtime source file—apart from the schema that validates local-path records—for an `imagesandvideo/` reference. A source import or direct runtime path therefore fails `pnpm content:check` before it can become a public-site dependency. The ledger still distinguishes Carlos's repository-publication instruction from file-level public-display, accessibility, provenance, and performance clearance; a fresh complete `pnpm verify` passes 216 tests, 13 routes, 12 manifest assets, zero public GLBs, and 26 client files.

- On 2026-07-25 Carlos declared sole creation of the local media, chose MIT for the code, and delegated the safest media launch path. Ledgers record all eight `imagesandvideo/` files as `owned` with status `ownership-recorded-runtime-hold` and runtime exclusion; the Observatory poster is approved for public production display; root `LICENSE` is MIT. `pnpm content:check` and the focused content-contract suite pass with the updated ownership and poster decisions.
- The local-media hold is now a file-level, hash-pinned clearance workflow rather than a group-only note. `docs/content/local-media-clearance-register.json` records all eight actual `imagesandvideo/` candidates with byte length, SHA-256, unknown-provenance/rights statement, clearance checklist, accessibility plan, and technical-review hold. The typed schema rejects duplicate identity, path, or hash records; kind/extension mismatches; and pending assets marked for inclusion. The content gate compares the record paths against the live directory and checks every file's size and hash; a temporary unregistered file was rejected by the exact-inventory diagnostic and removed. A dedicated contract proves the full inventory remains pending and excluded. A fresh `pnpm verify` passes formatting, zero-warning lint, strict TypeScript, all 217 tests, content/palette/server/asset gates, the 13-route production build, and the 12-manifest/0-public-GLB/26-client-file immersive scan; the evidence does not itself grant publication or runtime use.

## Intentionally deferred

- Real mobile water input, pointer/robot ripple and pause/visibility inspection, renderer draw/triangle counters, approved production 3D assets, and production-robot presentation.
- Live CC AI activation, user-approved public knowledge records, optional normalized streaming, and a durable shared limiter if production uses multiple function instances.
- Unverified project, remaining CV, music, video, travel, social, email, and metrics content.
- Live Selected Work population until Carlos approves the five-project order and contribution wording in U.4; the verified typographic fallback needs no project media.
- Expanded accessibility/end-to-end coverage, the first clean CI run, deployment, and release work.
